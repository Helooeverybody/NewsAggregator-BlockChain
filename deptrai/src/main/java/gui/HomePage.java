package gui;
import javax.swing.*;
import java.awt.*;
public class HomePage extends JPanel{
    private JButton trendDetection;
    private JButton searchEngine;
    public HomePage(){
        trendDetection = new JButton("TrendDetection");
        searchEngine = new JButton("SearchEngine");
        trendDetection.addActionListener(e ->{

        });
        searchEngine.addActionListener(e ->{

        });
    }
}
